nodejs security blog post
https://blog.risingstack.com/node-js-security-checklist/
https://blog.risingstack.com/node-js-security-tips/

Strict-Transport-Security
The HTTP Strict-Transport-Security response header (often abbreviated as HSTS)
lets a web site tell browsers that it should only be accessed using HTTPS, instead of using HTTP.
X-Frame-Options
 provides clickjacking protection
 The popup we are getting on page saying we won iphone. when we click on that that content must delete the email.
 to protect from such hack we can use clickjack.

X-XSS-Protection
The User login to the system and maintain the cookies. Attacker modifies the code with some intented click button
when The user clicks on the button enable the current session values.
Attacker now get the user session values now he can perform all the User actions.

X-XSS-Protection: 0 - Default dexable
X-XSS-Protection: 1 - enable the xss filtering and filter the part which got melicous code
X-XSS-Protection: 1; mode=block - prevent from rendring the page which is melicious
X-XSS-Protection: 1; report=<reporting-uri> - report to the url

Brute Force Protection
Brute force attack make the attacker to check the all possible scenario to break into the system using possible password.
we can use as a middleware to check both the possiblities.

var emailBasedRatelimit = ratelimit({
  db: redis.createClient(),
  duration: 60000,
  max: 10,
  id: function (context) {
    return context.body.email;
  }
});

var ipBasedRatelimit = ratelimit({
  db: redis.createClient(),
  duration: 60000,
  max: 10,
  id: function (context) {
    return context.ip;
  }
});

app.post('/login', ipBasedRatelimit, emailBasedRatelimit, handleLogin);
What we did here is that we have li


Nodejs fact
which  are the things popular, what people prefer
https://blog.risingstack.com/takeaways-node-js-survey-2018/

broadcast messages
 which allows you to send up to 50 messages with a single API request.
sending attachment of audio, video, image attachment
 which allows you to send up to 50 messages with a single API request.
 Quick reply
 Quick replies provide a way to present a set of up to 11 buttons in-conversation that contain a title and optional image, and appear prominently above the composer.

